import NavLinks from "../nav-links";
import Link from "next/link";

const Layout = ({ children }) => {
    return (
        <div>
            <NavLinks />
        </div>
    );
};

export default Layout;
